[pytest]
# Prevent .pytest_cache from being created in the project tree.
# The -p no:cacheprovider flag disables the cache plugin entirely.
addopts = -p no:cacheprovider
pythonpath = src
norecursedirs =
